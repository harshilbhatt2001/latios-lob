[package]
name = "latios-lob"
version = "0.1.0"
edition = "2024"

[dependencies]
bumpalo = "3.20.2"

[dev-dependencies]
criterion = { version = "0.8.2", features = ["html_reports", "real_blackbox", "csv_output"] }
