[package]
name = "latios-lob"
version = "0.1.0"
edition = "2024"

[dependencies]
bumpalo = "3.20.2"
fixed = "1.31.0"

[dev-dependencies]
criterion = { version = "0.8.2", features = ["html_reports", "real_blackbox", "csv_output"] }

[[bench]]
name = "order_book"
harness = false
